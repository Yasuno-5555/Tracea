[package]
name = "tracea-ffi"
version = "0.1.0"
edition = "2021"

[lib]
name = "tracea_ffi"
crate-type = ["staticlib", "cdylib"]

[dependencies]
tracea = { path = "../" }

[build-dependencies]
cbindgen = "0.26"
